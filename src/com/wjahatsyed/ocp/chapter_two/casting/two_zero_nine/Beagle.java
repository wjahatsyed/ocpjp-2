package com.wjahatsyed.ocp.chapter_two.casting.two_zero_nine;

/**
 * Created by Syed Wajahat on 10/13/2020.
 */
public class Beagle extends Dog implements Pet {
    /*
    Before you can tell whether the code is legal, you must know what the
    supertypes of this implementing class have declared. If any supertype in its inheritance
    has already provided concrete (that is, non-abstract) method implementations, then regardless of whether the supertype declares
    that it implements the interface, the subclass is under no obligation to reimplement (override) those methods.
     */
}
